/**
 * This file exists ONLY so that the `critters` package is
 * picked up by Next.js’s automatic dependency resolver.
 *
 * We don’t need to use the class here – exporting it is enough.
 */
import Critters from "critters"

export { Critters }
